# This block is invalid because the Docker credential helper protocol
# only allows passing a domain name when fetching credentials.
oci_credentials "example.com/foo" {
  docker_credentials_helper = "osxkeychain"
}
